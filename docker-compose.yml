services:
  dbt:
    build: .
    container_name: dbt
    working_dir: /usr/app/dbt
    volumes:
      - .:/usr/app/dbt
    env_file:
      - .env
    environment:
      DBT_PROFILES_DIR: /usr/app/dbt
    entrypoint: ["tail", "-f", "/dev/null"]
    tty: true
